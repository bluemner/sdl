SOURCE_FILES = driver.cpp
HEADER_FILES = na
COMPILER = g++
COMPILER_FLAGS = -w
LINKER_FLAGS = -lSDL2 -lGL -lGLU
OUTPUT_FILE = ../bin/run

all : $(SOURCE_FILES) 
	  $(COMPILER) $(SOURCE_FILES) $(COMPILER_FLAGS) $(LINKER_FLAGS) -o $(OUTPUT_FILE)
